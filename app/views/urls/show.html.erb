<p id="notice"><%= notice %></p>

<p>
  <strong>Url:</strong>
  <%= @url.url %>
</p>

<p>
  <strong>Doi:</strong>
  <%= @url.doi_id %>
</p>

<%= link_to 'Edit', edit_doi_url_path(@url) %> |
<%= link_to 'Back', doi_urls_path %>
